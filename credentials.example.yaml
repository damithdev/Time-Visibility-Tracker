# Time Visibility Tracker Credentials
# Copy this file to ~/.tvt/credentials.yaml
# IMPORTANT: Never commit credentials.yaml to version control!

# Slack integration
# Get your token from https://api.slack.com/apps
slack:
  user_token: xoxp-your-token-here

# Teams integration (coming in Phase 1)
# teams:
#   client_id: your-azure-client-id
#   client_secret: your-azure-client-secret
#   tenant_id: your-tenant-id
